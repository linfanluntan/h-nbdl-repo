# Paper LaTeX Source

Compile with:
```bash
cd paper/
cp ../figures/*.png .   # or generate with: python ../scripts/figures/paper_figures.py
pdflatex main.tex
pdflatex main.tex  # twice for references
```

